Insertion sort algi=orithm analysis :

Algorithm :

for j = 1 to length (array) :
    key = array[j]
    i = j-1

    while i>=0 and array[i] > key :
        array[i+1] = array[i]
        i = i-1

    array[i+1] = key


Best case :

input : 1,2,3 

